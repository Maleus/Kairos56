baseURL = "https://www.kairos56.com/"
locale = "en-us"
title = "Kairos 56"
theme = "kairos"

# All chapter-specific content lives in data/chapter.yaml — edit that,
# not this file, when redeploying for a new chapter. Only baseURL and
# title need to change here. The team password is stored in Cloudflare
# KV (set via the admin page), never in this repo.

[params]
  description = "Kairos Prison Ministry — WTSP Chapter"
